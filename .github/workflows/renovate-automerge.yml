---
name: Renovate Auto Merge

on:
    pull_request:

jobs:
    lint:
        uses: ./.github/workflows/automerge-global.yml
        with:
            author-name: leiicamundi
        secrets: inherit
