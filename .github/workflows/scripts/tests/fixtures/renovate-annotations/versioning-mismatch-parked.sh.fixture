# Parked: pre-GA dev chart tag until the release.
# renovate: datasource=helm depName=camunda-platform versioning=regex:^15(\.(?<minor>\d+))?(\.(?<patch>\d+))?$ renovate-inert-ok
export CHART_VERSION="15-dev-latest"
